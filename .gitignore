# Dependencias
node_modules/
vendor/

# Entorno y Secretos
.env
.env.backup
certs/*.key
certs/*.crt

# Docker y Bases de Datos
services/mysql/data/*
!services/mysql/data/.gitkeep
devtools/minio_data/*

# Logs y temporales
*.log
.phpunit.result.cache
.pnpm-store/

# --- Backends ---
# Ignoramos todo el contenido de las carpetas
backends/landing/*
backends/api-shared/*

# EXCEPTUAMOS los archivos .gitkeep para que las carpetas NO desaparezcan de GitHub
!backends/landing/.gitkeep
!backends/api-shared/.gitkeep

# --- Backends ---
backends/landing/*
backends/api-shared/*
!backends/landing/.gitkeep
!backends/api-shared/.gitkeep

# --- Frontends ---
# Bloqueamos el contenido (Laravel de Tenant y Landlord) 
# porque cada uno tendrá su propio repo independiente.
frontends/tenant/*
frontends/landlord/*

# Mantenemos las carpetas en la estructura del orquestador
!frontends/tenant/.gitkeep
!frontends/landlord/.gitkeep

# --- Seguridad Extra (Certificación) ---
# Ignora cualquier certificado o llave que se genere en el futuro
certs/*.pem
certs/*.key
certs/*.crt
!certs/.gitkeep