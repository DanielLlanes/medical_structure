# --- Dependencias y Temporales ---
node_modules/
vendor/
*.log
.phpunit.result.cache
.pnpm-store/

# --- Seguridad y Secretos (Vital para Certificación) ---
.env
.env.backup
certs/*.key
certs/*.crt
certs/*.pem
!certs/.gitkeep

# --- Datos Persistentes (Docker) ---
services/mysql/data/*
!services/mysql/data/.gitkeep
devtools/minio_data/*
services/redis/dump.rdb
dump.rdb

# --- ESTRUCTURA DE REPOS INDEPENDIENTES ---
# Bloqueamos el contenido de las apps porque cada una tiene su propio Git.
# Solo mantenemos la carpeta y el .gitkeep en este orquestador.

# 1. Backends
backends/api-shared/*
!backends/api-shared/.gitkeep

# 2. Frontends
# Ignoramos el código de los tres proyectos (Tenant, Landlord y Landing)
frontends/tenant/*
frontends/landlord/*
frontends/landing/app/*

# --- LO QUE SÍ SE QUEDA (Configuración y Estructura) ---
# Mantenemos los .gitkeep para que Git respete la existencia de las carpetas
!frontends/tenant/.gitkeep
!frontends/landlord/.gitkeep
!frontends/landing/app/.gitkeep

# Mantenemos las carpetas common (Configuración de Docker, PHP, Nginx)
# Estas carpetas SÍ pertenecen a este repositorio maestro.
!frontends/common/
!frontends/landing/common/
!backends/common/

# --- Otros ---
.DS_Store
Thumbs.db