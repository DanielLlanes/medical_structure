# --- Dependencias y Temporales ---
node_modules/
vendor/
*.log
.phpunit.result.cache
.pnpm-store/

# --- Seguridad y Secretos (Vital para Certificación) ---
.env
.env.backup
certs/*.key
certs/*.crt
certs/*.pem
!certs/.gitkeep

# --- Datos Persistentes (Docker) ---
services/mysql/data/*
!services/mysql/data/.gitkeep
devtools/minio_data/*

# --- ESTRUCTURA DE REPOS INDEPENDIENTES ---
# Bloqueamos el contenido de las apps porque cada una tiene su propio Git.
# Solo mantenemos la carpeta y el .gitkeep en este orquestador.

# 1. Landing (Ahora en la raíz)
landing/*
!landing/.gitkeep
!landing/common/

# 2. Backends
backends/api-shared/*
!backends/api-shared/.gitkeep

# 3. Frontends (Tenants y Landlord)
frontends/tenant/*
frontends/landlord/*
!frontends/tenant/.gitkeep
!frontends/landlord/.gitkeep

# Redis
services/redis/dump.rdb
dump.rdb

# --- NOTA ---
# No ignoramos las carpetas 'common/' porque ahí vive la configuración 
# de Docker que SÍ pertenece a este repositorio de estructura.